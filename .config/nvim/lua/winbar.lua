--[[
Behaviors:
- Returns an empty placeholder table consumed by the treesitter-context setup in `plugins.lua`.
- Adds no winbar behavior by itself.
]]
-- treesitter-context loads this table from plugins.lua after the plugin is ready.
return {}
